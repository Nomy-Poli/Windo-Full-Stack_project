@*@Html.AntiForgeryToken()*@

<div class="row">
    <div class="col">
        <div class="card mt-3 text-center mb-5">
            <div class="card-header">
                <div class="float-right"><h4>משתמשי ישומיים</h4></div>
                <div class="float-left"> <button type="button" class="btn btn-primary" onclick="addNewUser()"><i class="fa fa-plus"></i> משתמש חדש</button></div>
            </div>
            <div class="card-body">
                <div id="usersTable"></div>
            </div>
        </div>
    </div>
</div>

<!-- Modal Delete Role-->
<div class="modal fade" id="DeleteUserModal" role="dialog" aria-labelledby="DeleteUserModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <form>
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="DeleteUserModalLabel"><i class="fa fa-clipboard-check"></i></h5>
                    <button type="button" class="close ml-0" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">לא</button>
                    <button type="button" class="btn btn-primary" onclick="confirmDeleteUser()">כן</button>
                </div>

            </div>
        </form>
    </div>
</div>

@await Html.PartialAsync("_AddUserLayout")
@await Html.PartialAsync("_EditUserLayout")