import { WhenItWasPipe } from './when-it-was.pipe';

describe('WhenItWasPipe', () => {
  it('create an instance', () => {
    const pipe = new WhenItWasPipe();
    expect(pipe).toBeTruthy();
  });
});
